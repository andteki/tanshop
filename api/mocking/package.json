{
  "name": "mocking",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "hai-server --watch database.json --auth auth.json --routes routes.json --port 7000",
    "json": "json-server --watch database.json --port 7000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "hai-server": "^0.0.4"
  }
}
